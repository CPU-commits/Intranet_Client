export enum PresenceStatusKeys {
	OUT = 'out',
	ABSENT = 'absent',
	PRESENT = 'present',
	RETIRED = 'retired',
	UNKNOWN = 'unknown',
}

export enum PresenceStatus {
	'out',
	'absent',
	'present',
	'retired',
	'unknown',
}
